{
  "scripts": {
    "prisma:generate": "pnpm exec prisma generate",
    "db:push": "pnpm exec prisma db push",
    "db:seed": "pnpm exec tsx --tsconfig tsconfig.scripts.json scripts/seed.ts",
    "dev": "next dev -p 3013 -H 127.0.0.1"
  }
}
